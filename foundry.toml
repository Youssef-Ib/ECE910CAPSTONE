[profile.default]
src = 'contracts'
out = 'out'
libs = ['lib']
solc_version = '0.8.23'
optimizer = true
optimizer_runs = 200

# NOTE: This repo is primarily Hardhat-based.
# You can still use Foundry for fuzz/invariant testing by:
#  1) running `forge install OpenZeppelin/openzeppelin-contracts`
#  2) mapping imports (see README)
